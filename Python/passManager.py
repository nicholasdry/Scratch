import sys
import pyperclip

def hashPass(password):
	output = ""
	for x in range(0, len(password)):

# Add in a new password to the dictionary, it will be in the third index of argv

if sys.argv[1] == "add":
	print("add")	
